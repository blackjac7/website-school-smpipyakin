# ==============================================================================
# ENVIRONMENT VARIABLES EXAMPLE
# ==============================================================================
# Instructions:
# 1. Copy this file to `.env` (for local dev) or `.env.production` (for prod).
# 2. Replace the placeholder values with your actual secrets.
# 3. NEVER commit your `.env` file to version control.
# ==============================================================================

# =========================================
# 1. COMMON SETTINGS (Shared across envs)
# =========================================

# Node Environment
# Options: "development", "production", "test"
# Default: "development"
NODE_ENV="development"

# App Base URL
# Used for SEO, redirects, and canonical links.
# Local: "http://localhost:3000"
# Staging/Prod: "https://your-domain.com"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# File Upload Limits (in bytes)
# Default: 5242880 (5MB)
MAX_FILE_SIZE="5242880"

# -----------------------------------------
# EXTERNAL SERVICES (Third-Party APIs)
# -----------------------------------------

# Cloudinary (Main Media Storage)
# Dashboard: https://cloudinary.com/console
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="your_cloud_name"
CLOUDINARY_API_KEY="your_api_key"
CLOUDINARY_API_SECRET="your_api_secret"
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET="your_upload_preset"

# Cloudinary (PPDB Specific - Optional)
# If using a separate Cloudinary account/bucket for student registrations
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME_PPDB="your_ppdb_cloud_name"
CLOUDINARY_API_KEY_PPDB="your_ppdb_api_key"
CLOUDINARY_API_SECRET_PPDB="your_ppdb_api_secret"

# EmailJS (Transactional Emails)
# Dashboard: https://dashboard.emailjs.com/
NEXT_PUBLIC_EMAILJS_SERVICE_ID="your_service_id"
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID="your_template_id"
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY="your_public_key"

# Cloudflare R2 (Object Storage - Optional)
# Used for document storage if configured
R2_ACCOUNT_ID="your_cloudflare_account_id"
R2_ACCESS_KEY_ID="your_r2_access_key_id"
R2_SECRET_ACCESS_KEY="your_r2_secret_access_key"
R2_BUCKET_NAME="your_bucket_name"
R2_PUBLIC_URL="https://pub-your-id.r2.dev"

# Analytics (Optional)
# Set to "1" to enable Vercel Speed Insights in production
NEXT_PUBLIC_VERCEL_SPEED_INSIGHTS=""

# =========================================
# 2. ENVIRONMENT SPECIFIC (Change per env)
# =========================================

# -----------------------------------------
# A. STAGING / TESTING / DEVELOPMENT
# -----------------------------------------

# Database Connection
# Notes:
# - For Local Dev: You can use SQLite "file:./dev.db" if supported, but PostgreSQL is recommended.
# - For Staging: Use a separate PostgreSQL database than production.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/school_db_dev?schema=public"

# Direct Database Connection
# Required for migrations if using a connection pooler (like Supabase Transaction Mode).
# If not using a pooler, this should be identical to DATABASE_URL.
DIRECT_URL="postgresql://postgres:password@localhost:5432/school_db_dev?schema=public"

# Security Secrets
# - JWT_SECRET: Used for signing auth tokens.
# - CRON_SECRET: Used to secure API endpoints for cron jobs.
# Generate new random strings: `openssl rand -base64 32`
JWT_SECRET="dev_secret_key_change_this"
CRON_SECRET="dev_cron_secret"


# -----------------------------------------
# B. PRODUCTION (Reference Only)
# -----------------------------------------
# DO NOT UNCOMMENT THESE IN LOCAL DEV.
# These variables should be set in your deployment platform (Vercel, Railway, Coolify, etc.)

# DATABASE_URL="postgresql://user:password@prod-db-host:5432/school_db_prod?pgbouncer=true"
# DIRECT_URL="postgresql://user:password@prod-db-host:5432/school_db_prod"

# JWT_SECRET="<long_random_secure_string>"
# CRON_SECRET="<long_random_secure_string>"

# NEXT_PUBLIC_APP_URL="https://www.yourschool.sch.id"
# NODE_ENV="production"
